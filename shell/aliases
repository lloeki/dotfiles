## .aliases

case "$OSTYPE" in
    *darwin*)
        alias top="top -o cpu -s 5"
        ;;
    *linux*)
        alias ls="ls --color"
        ;;
esac

alias ll="ls -l"
alias tree="tree -C"
which colordiff 2>&1 >/dev/null && alias diff="colordiff"

