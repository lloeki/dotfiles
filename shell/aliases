## .aliases

case "$OSTYPE" in
    *darwin*)
        alias top="top -o cpu -s 5"
        ;;
    *linux*)
        alias ls="ls --color"
        ;;
esac

alias ll="ls -l"
alias tree="tree -C"
if command -v colordiff 2>&1 >/dev/null; then
    alias diff="colordiff"
fi

# vim: ft=bash
